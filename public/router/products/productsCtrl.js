define(['app'], function(app){
  app.registerController('productsGridCtrl', ['$scope', function($scope){
    angular.extend($scope, {

    });
  }]);

   app.registerController('productsListCtrl', ['$scope', function($scope){

   }]);

   app.registerController('prodcutCtrl', ['$scope', function($scope){

   }]);
});